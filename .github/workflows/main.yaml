name: Cypress Tests

on: [push]

jobs:
  cypress-run:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout

      - name: Cypress run
        uses: cypress-io/github-action
        with:
          build: npm run build
          start: npm start
